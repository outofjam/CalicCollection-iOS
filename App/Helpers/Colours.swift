import SwiftUI

extension Color {
    // Brand colors (using system defaults)
    static let calicoPrimary = Color.accentColor
    static let calicoSecondary = Color.secondary
    
    // Status colors
    static let calicoSuccess = Color.green
    static let calicoWarning = Color.orange
    static let calicoError = Color.red
    
    // Text hierarchy
    static let calicoTextPrimary = Color.primary
    static let calicoTextSecondary = Color.secondary
}
